import static org.mockito.Mockito.*;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;

import com.fasterxml.jackson.databind.ObjectMapper;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import org.springframework.http.MediaType;
import org.springframework.test.web.servlet.MockMvc;
import org.springframework.test.web.servlet.setup.MockMvcBuilders;

import java.util.Arrays;
import java.util.List;

import IMPORTS_SECTION;

/**
 * this class tests the %Controller class
 * these tests we want to run in order
 * @author ^
 *
 */
@ExtendWith(MockitoExtension.class)
public class %ControllerTest {

	/**
	 * generate a random port for testing
	 */
	private MockMvc mockMvc;

    @Mock
    private %Controller &Controller;

	/**
	 * create a Mock Business object
	 */
	@InjectMocks
	private %Service &Service;

    private ObjectMapper objectMapper = new ObjectMapper();

	@BeforeEach
    void setUp() {
        mockMvc = MockMvcBuilders.standaloneSetup(&Controller).build();
    }

	/**
	 * test creating a new %
	 * @throws 
	 */
	@Test
	public void testA_Create%() throws Exception
	{
	    %DTO & = generateRandom%();
        when(&Service.create%(any(%DTO.class))).thenReturn(&);

        mockMvc.perform(post("/###/create")
                .contentType(MediaType.APPLICATION_JSON)
                .content(objectMapper.writeValueAsString(&)))
                .andExpect(status().isCreated());
    }

    /**
	 * test getting all %
	 * @throws 
	 */
	@Test
	public void testB_GetAll%() throws Exception
	{
		List<%DTO> &s = Arrays.asList(generateRandom%(), generateRandom%());
        when(&Service.getAll%()).thenReturn(&s);

        mockMvc.perform(get("/###/all"))
                .andExpect(status().isOk());
	}

	/**
	 * test getting a single % by primary key
	 * @throws 
	 */
	@Test
	public void testC_Get%() throws Exception
	{
		%DTO & = generateRandom%();
        when(&Service.find%ById(1)).thenReturn(&);

        mockMvc.perform(get("/###/findById/1"))
                .andExpect(status().isOk());
	}

    /**
	 * test updating a %
	 * @throws 
	 */
	@Test
	public void testD_Update%() throws Exception
	{
	    %DTO & = generateRandom%();
        when(&Service.update%(any(%DTO.class))).thenReturn(&);

        mockMvc.perform(post("/###/update")
                .contentType(MediaType.APPLICATION_JSON)
                .content(objectMapper.writeValueAsString(&)))
                .andExpect(status().isOk());
	}

	/**
	 * test deleting a %
	 * @throws 
	 */
	@Test
	public void testE_Delete%() throws Exception
	{
		doNothing().when(&Service).delete%(1);

        mockMvc.perform(delete("/###/delete/1"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.message").value("successfully deleted"));
	}

    FKSECTION


	public static %DTO generateRandom%() {
		RANDOM_DTO_GENERATOR
	}
}