import static org.mockito.Mockito.*;

import java.util.*;
import static org.junit.jupiter.api.Assertions.*;
import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import org.junit.jupiter.api.extension.ExtendWith;
import org.mockito.InjectMocks;
import org.mockito.Mock;
import org.mockito.junit.jupiter.MockitoExtension;
import $.dtos.%DTO;
import $.pojos.%;
import $.repositories.%Repository;
import $.exceptions.ResourceNotFoundException;
import $.utilities.Randomizer;

/**
 * this class retrieves data from the controller class
 * most business logic should be put in this class
 * @author ^
 *
 */
@ExtendWith(MockitoExtension.class)
class %ServiceTest {

    @Mock
    private %Repository &Repository;

    @InjectMocks
    private %Service &Service;

    private % &;
    private %DTO &DTO;

    @BeforeEach
    void setUp() {
        & = generateRandom%Entity();
        &DTO = generateRandom%();
    }

    @Test
    void testGetAll%() throws Exception {
        when(&Repository.findAll()).thenReturn(Collections.singletonList(&));

        List<%DTO> result = &Service.getAll%();

        assertEquals(1, result.size());
    }

    @Test
    void testFind%ById_Found() throws Exception {
        when(&Repository.findById(1)).thenReturn(Optional.of(&));

        %DTO result = &Service.find%ById(1);

        assertNotNull(result);
    }

    @Test
    void testFind%ById_NotFound() {
        when(&Repository.findById(1)).thenReturn(Optional.empty());

        Exception exception = assertThrows(ResourceNotFoundException.class, () -> {
            &Service.find%ById(1);
        });

        assertEquals("id = 1 not found", exception.getMessage());
    }

    @Test
    void testCreate%() {
        when(&Repository.save(any(%.class))).thenReturn(&);

        %DTO result = &Service.create%(&DTO);

        assertNotNull(result);
    }

    @Test
    void testUpdate%_Found() throws Exception {
        when(&Repository.findById(1)).thenReturn(Optional.of(&));
        when(&Repository.save(any(%.class))).thenReturn(&);

        %DTO result = &Service.update%(&DTO);

        assertNotNull(result);
    }

    @Test
    void testUpdate%_NotFound() {
        when(&Repository.findById(1)).thenReturn(Optional.empty());

        Exception exception = assertThrows(ResourceNotFoundException.class, () -> {
            &Service.update%(&DTO);
        });

        assertEquals("id = 1 not found", exception.getMessage());
    }

    @Test
    void testDelete%_Found() throws Exception {
        when(&Repository.findById(1)).thenReturn(Optional.of(&));
        doNothing().when(&Repository).deleteById(1);

        String result = &Service.delete%(1);

        assertEquals("Successfully Deleted", result);
    }

    @Test
    void testDelete%_NotFound() {
        when(&Repository.findById(1)).thenReturn(Optional.empty());

        Exception exception = assertThrows(ResourceNotFoundException.class, () -> {
            &Service.delete%(1);
        });

        assertEquals("id = 1 not found", exception.getMessage());
    }

    public static %DTO generateRandom%() {
		RANDOM_DTO_GENERATOR
	}
    public static % generateRandom%Entity() {
		RANDOM_POJO_GENERATOR
	}

    FKSECTION
}
